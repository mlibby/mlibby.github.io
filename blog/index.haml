---
layout: default
title: My Blog
---
%main
  %h1 
    Blog Posts
  %ul
  - require "jekyll"
  - opts = Jekyll.configuration {}
  - site = Jekyll::Site.new opts
  - site.read
  - site.posts.docs.reverse.each do |post|
    %li
      %a{ href: post.url }
        = post.data["title"]
        = "(#{post.data["date"].to_date})"
